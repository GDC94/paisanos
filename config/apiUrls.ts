export const API_URLS = {
  PROJECTS: `${process.env.NEXT_PUBLIC_BASE_API_URL}/projects`,
  PROJECTS_BY_SLUG: `${process.env.NEXT_PUBLIC_BASE_API_URL}/project`,
};

export const DATA_URL = {
  PROJECTS: `${process.env.NEXT_PUBLIC_BASE_SITE_URL}/data/projects.json`,
};
